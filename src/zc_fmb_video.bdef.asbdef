projection implementation in class ZBP_C_FMB_VIDEO unique;
strict ( 2 );
use draft;
define behavior for ZC_FMB_VIDEO alias ZcFmbVideo
use etag

{
  use create;
  use update;
  use delete;

  use event AssignUrlFinished;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action PopUpConfirm;
  use action AssignUrl;
  use action AssignUrlFinish;
}